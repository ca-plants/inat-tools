/** @type {import('jest').Config} */
const config = {
    moduleNameMapper: {
        "https://cdn.jsdelivr.net/npm/which-polygon@2.2.1": "which-polygon",
        "https://cdn.skypack.dev/d3-geo": "d3-geo",
    },
};

export default config;
